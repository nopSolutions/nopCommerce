@using Nop.Web.Framework;
@model Nop.Web.MVC.Areas.Admin.Models.CurrencyModel
@using (Html.BeginForm())
{
    <div class="section-header">
        <div class="title">
            <img src="@Url.Content("~/Areas/Admin/Content/images/ico-catalog.png")" />
            @T("Admin.Catalog.Currencies.AddNew") - @Html.ActionLink("(" + T("Admin.Catalog.Currencies.BackToList") + ")", "List")
        </div>
        <div class="options">
            <input type="submit" value="@T("Admin.Common.Save")" class="t-button" />
        </div>
    </div>
    @Html.Partial("_CreateOrUpdate", Model)
}