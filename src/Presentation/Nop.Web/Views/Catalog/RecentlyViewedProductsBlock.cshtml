@model IList<ProductOverviewModel>
@using Nop.Core;
@using Nop.Core.Infrastructure;
@using Nop.Web.Models.Catalog;
@if (Model.Count > 0)
{
    <div class="block block-recently-viewed-products">
        <div class="title">
            @T("Products.RecentlyViewedProducts")
        </div>
        <div class="clear">
        </div>
        <div class="listbox">
            <ul>
                @for (int i = 0; i < Model.Count; i++)
                {
                    var product = Model[i];
                    <li><a href="@Url.RouteUrl("Product", new { productId = product.Id, SeName = product.SeName })">@product.Name</a>
                    </li>
                    if (i != Model.Count - 1)
                    {
                        <li class="separator">&nbsp;</li>
                    }
                }
            </ul>
        </div>
    </div>
}