@model Nop.Web.Models.Customer.PasswordRecoveryModel
@{
    Layout = "~/Views/Shared/_Root.cshtml";

    //title
    Html.AddTitleParts(T("PageTitle.PasswordRecovery").Text);
}
<div class="password-recovery-page">
    <div class="page-title">
        <h1>@T("Account.PasswordRecovery")</h1>
    </div>
    <div class="clear">
    </div>
    <div class="body">
        @if (!String.IsNullOrEmpty(Model.Result))
        {
            <strong>
                @Model.Result
            </strong>
        }
        @using (Html.BeginForm())
        {
            <div>
                @T("Account.PasswordRecovery.Tooltip")
            </div>
            <div>
                @Html.LabelFor(model => model.Email)
                @Html.TextBoxFor(model => model.Email, new { @class = "email" })
                @Html.ValidationMessageFor(model => model.Email)
            </div>
            <input type="submit" id="send-email" name="send-email" value="@T("Account.PasswordRecovery.RecoverButton")" />
                
        }
    </div>
</div>
