@{
    var openAuthParameters = ExternalAuthorizerHelper.RetrieveParametersFromRoundTrip(false);
}
@using Nop.Services.Authentication.External;
@if (openAuthParameters != null)
{
    <div class="external-auth-association">
        Account Association: Your new user account will be linked to <span class="identifier">@openAuthParameters.ExternalIdentifier</span>
            <a href="@Url.Action("RemoveParameterAssociation", "ExternalAuthentication", new { ReturnUrl = Request.Url.ToString() })" class="remove">
                (remove)</a>            
    </div>
}