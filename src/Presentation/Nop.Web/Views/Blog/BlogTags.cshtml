@model IList<BlogPostTagModel>
@using Nop.Web.Models.Blogs
@if (Model.Count > 0)
{
    <div class="block block-popular-blogtags">
        <div class="title">
            @T("Blog.TagCloud")
        </div>
        <div class="clear">
        </div>
        <div class="listbox">
            <div class="tags">
                <ul>
                    @foreach (var item in Model)
                    {
                        <li><a href="@Url.RouteUrl("BlogByTag", new { tag = item.Name })" style="font-size:@(item.FontSizePercent)%;">@item.Name</a>
                        </li>
                    }
                </ul>
            </div>
        </div>
    </div>
}