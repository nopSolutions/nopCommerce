@model PollModel
<div class="block block-poll">
    <div class="title">
        <strong>@T("Polls.Title")</strong>
    </div>
    <div class="listbox">
        @{
            await Html.RenderPartialAsync("_Poll", Model);
        }
    </div>
</div>
