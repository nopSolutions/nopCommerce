@model System.Decimal
@using Telerik.Web.Mvc.UI

@(Html.Telerik().NumericTextBox<decimal>()
        .Name(ViewData.TemplateInfo.GetFullHtmlFieldName(string.Empty))
        .Value(Model)
        .DecimalDigits(8)   //Display 8 digits
)

