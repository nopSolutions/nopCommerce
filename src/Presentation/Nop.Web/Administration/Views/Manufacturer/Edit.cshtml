@model ManufacturerModel

@{
    //page title
    ViewBag.Title = T("Admin.Catalog.Manufacturers.EditManufacturerDetails").Text;

    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("Manufacturers");
}

@using (Html.BeginForm(null, null, FormMethod.Post, new { id = "manufacturer-form" }))
{
    @Html.AntiForgeryToken()
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Admin.Catalog.Manufacturers.EditManufacturerDetails") - @Model.Name
            <small>@Html.ActionLink("(" + T("Admin.Catalog.Manufacturers.BackToList") + ")", "List")</small>
        </h1>
        <div class="pull-right">
            <input type="submit" value="@T("Admin.Common.Preview")" onclick="javascript:OpenWindow('@Url.RouteUrl("Manufacturer", new {SeName = Model.SeName})', 800, 600, true); return false;" class="btn bg-aqua" />
            <input type="submit" name="save" class="btn bg-blue" value="@T("Admin.Common.Save")" />
            <input type="submit" name="save-continue" class="btn bg-blue" value="@T("Admin.Common.SaveContinue")" />
            <span id="manufacturer-delete" class="btn bg-red">
                <i class="fa fa-trash"></i>
                @T("Admin.Common.Delete")
            </span>
        </div>
    </div>
    @Html.Partial("_CreateOrUpdate", Model)
}
@Html.DeleteConfirmation("manufacturer-delete")
