@model ReturnRequestModel

@{
    //page title
    ViewBag.Title = T("Admin.ReturnRequests.EditReturnRequestDetails").Text;
}

@using (Html.BeginForm())
{
    @Html.AntiForgeryToken()
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Admin.ReturnRequests.EditReturnRequestDetails") 
            <small>@Html.ActionLink("(" + T("Admin.ReturnRequests.BackToList") + ")", "List")</small>
        </h1>
        <div class="pull-right">
            <input type="submit" name="notify-customer" class="btn btn-default" value="@T("Admin.ReturnRequests.NotifyCustomer")" />
            <input type="submit" name="save" class="btn btn-default" value="@T("Admin.Common.Save")" />
            <input type="submit" name="save-continue" class="btn btn-default" value="@T("Admin.Common.SaveContinue")" />
            <span id="returnrequest-delete" class="btn btn-default">@T("Admin.Common.Delete")</span>
        </div>
    </div>
    @Html.Partial("_CreateOrUpdate", Model)
}
@Html.DeleteConfirmation("returnrequest-delete")