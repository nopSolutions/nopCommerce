@model DeliveryDateModel

@{
    //page title
    ViewBag.Title = T("Admin.Configuration.Shipping.DeliveryDates.AddNew").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("Delivery dates");
}

<form asp-controller="Shipping" asp-action="CreateDeliveryDate" method="post">
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Admin.Configuration.Shipping.DeliveryDates.AddNew")
            <small>
                <i class="fa fa-arrow-circle-left"></i>
                <a asp-action="DatesAndRanges">@T("Admin.Configuration.Shipping.DeliveryDates.BackToList")</a>
            </small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
            <button type="submit" name="save-continue" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.SaveContinue")
            </button>
            @await Component.InvokeAsync("AdminWidget", new { widgetZone = AdminWidgetZones.DeliveryDateDetailsButtons })
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdateDeliveryDate", Model)
</form>