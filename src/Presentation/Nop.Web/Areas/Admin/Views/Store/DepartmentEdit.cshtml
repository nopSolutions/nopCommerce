@model StoreDepartmentModel

@{
    //page title
    ViewBag.PageTitle = T("Admin.Stores.Stores.Department.EditDepartment").Text;
    //active menu item (system name)
    Html.SetActiveMenuItemSystemName("Store list");
}

<form asp-controller="Store" asp-action="DepartmentEdit" method="post">
    <div class="content-header clearfix">
        <h1 class="pull-left">
            @T("Admin.Stores.Stores.Department.EditDepartment")
            <small>
                <i class="fa fa-arrow-circle-left"></i>
                <a asp-action="Edit" asp-route-id="@Model.StoreId">@T("Admin.Stores.Stores.Department.BackToStore")</a>
            </small>
        </h1>
        <div class="pull-right">
            <button type="submit" name="save" class="btn bg-blue">
                <i class="fa fa-floppy-o"></i>
                @T("Admin.Common.Save")
            </button>
            @await Component.InvokeAsync("AdminWidget", new { widgetZone = AdminWidgetZones.EmployeeAddressDetailsButtons, additionalData = Model })
        </div>
    </div>
    @await Html.PartialAsync("_CreateOrUpdateDepartment", Model)
</form>