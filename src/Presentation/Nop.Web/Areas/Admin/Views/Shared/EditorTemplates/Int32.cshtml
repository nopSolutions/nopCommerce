@model Int32

@Html.TextBoxFor(model => model)
@{
    var postfix = "";
    if (ViewData.ContainsKey("postfix") && ViewData["postfix"] != null)
    {
        postfix = ViewData["postfix"].ToString();
    }
}
<script>
    $(document).ready(function() {
        @*TODO valid method temporary commented. wrong one is below. We have to use GetFullHtmlFieldId
        $("#@ViewData.TemplateInfo.GetFullHtmlFieldId(string.Empty)").TouchSpin({*@
        $("#@ViewData.TemplateInfo.GetFullHtmlFieldName(string.Empty)").TouchSpin({
            min: @(int.MinValue),
            max: @(int.MaxValue),
            decimals: 0,
            verticalbuttons: true,
            postfix: "@postfix"
        });
    });
</script>