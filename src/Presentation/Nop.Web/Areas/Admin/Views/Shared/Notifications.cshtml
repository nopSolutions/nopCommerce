@{
    //success messages
    var successMessages = new List<string>();
    if (TempData[$"{NopNotificationsDefaults.Prefix}{NotifyType.Success}"] != null)
    {
        successMessages.AddRange(TempData[$"{NopNotificationsDefaults.Prefix}{NotifyType.Success}"] as IList<string>);
    }
    if (ViewData[$"{NopNotificationsDefaults.Prefix}{NotifyType.Success}"] != null)
    {
        successMessages.AddRange(ViewData[$"{NopNotificationsDefaults.Prefix}{NotifyType.Success}"] as IList<string>);
    }


    //error messages
    var errorMessages = new List<string>();
    if (TempData[$"{NopNotificationsDefaults.Prefix}{NotifyType.Error}"] != null)
    {
        errorMessages.AddRange(TempData[$"{NopNotificationsDefaults.Prefix}{NotifyType.Error}"] as IList<string>);
    }
    if (ViewData[$"{NopNotificationsDefaults.Prefix}{NotifyType.Error}"] != null)
    {
        errorMessages.AddRange(ViewData[$"{NopNotificationsDefaults.Prefix}{NotifyType.Error}"] as IList<string>);
    }


    //warning messages
    var warningMessages = new List<string>();
    if (TempData[$"{NopNotificationsDefaults.Prefix}{NotifyType.Warning}"] != null) {
        warningMessages.AddRange(TempData[$"{NopNotificationsDefaults.Prefix}{NotifyType.Warning}"] as IList<string>);
    }
    if (ViewData[$"{NopNotificationsDefaults.Prefix}{NotifyType.Warning}"] != null) {
        warningMessages.AddRange(ViewData[$"{NopNotificationsDefaults.Prefix}{NotifyType.Warning}"] as IList<string>);
    }
}

@foreach (var message in successMessages)
{
    <div class="alert alert-success alert-dismissable">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
        @message
    </div>
}

@foreach (var message in errorMessages)
{
    <div class="alert alert-danger alert-dismissable">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
        @message
    </div>
}

@foreach (var message in warningMessages) {
    <div class="alert alert-warning alert-dismissable">
        <button type="button" class="close" data-dismiss="alert" aria-hidden="true">&times;</button>
        @message
    </div>
}