@model ActionAlertModel

<div class="modal-dialog">
    <div class="modal-content">
        <div class="modal-header">
            <h4 class="modal-title" id="@(Model.WindowId)-title">@T("Admin.Common.Alert")</h4>
            <button type="button" class="btn-close" data-bs-dismiss="modal" aria-label="Close"></button>
        </div>
        <div class="modal-body">
            <div class="additional-text" id="@(Model.AlertId)-info"></div>
            @(Model.AlertMessage)
        </div>
        <div class="modal-footer">
            <button type="button" class="btn btn-primary float-right" data-bs-dismiss="modal">@T("Admin.Common.Ok")</button>
        </div>
    </div>
</div>
@*The closing tag must be described in full, otherwise, some browsers incorrectly render the markup, without taking into account the closing tag.*@
<div class="btn btn-default" id="@(Model.AlertId)" style="display:none"></div>