@{
    Layout = "";
}
@model PublicInfoModel
@using Nop.Plugin.Widgets.TestTask.Models;

@if (!string.IsNullOrEmpty(Model.PromoMessage))
{
    <div class="page-title">
        <h1>@Html.Raw(Model.PromoMessage)<h1/>
    </div>
}