@model IList<AbcWarehouse.Plugin.Widgets.Listrak.Models.ListrakOrderItem>

<!-- Listrak: Updates cart -->
<script type="text/javascript">
    (function(d){if(typeof _ltk=="undefined"){if(document.addEventListener)document.addEventListener("ltkAsyncListener",function(){_ltk_util.ready(d)});else{e=document.documentElement;e.ltkAsyncProperty=0;e.attachEvent("onpropertychange",function(e){if(e.propertyName=="ltkAsyncProperty"){_ltk_util.ready(d)}})}}else{_ltk_util.ready(d)}})(function(){
        if (!Model.Any()) {
            _ltk.SCA.ClearCart();
        }
        else {
            @foreach (var item in Model)
            {
                <text>_ltk.SCA.AddItemWithLinks('@item.Sku', @item.Quantity, @item.Price, '@item.Title', '@item.ImageUrl', '@item.ProductUrl');</text>
            }

            _ltk.SCA.Submit();
        }
    });
</script>
