@using Nop.Plugin.Misc.WebApi.Frontend
@{
    Layout = "_ConfigurePlugin";
    NopHtml.SetActiveMenuItemSystemName(WebApiFrontendDefaults.SystemName);
}

<h1>Web API Frontend Plugin</h1>

<div class="alert alert-info">
    <strong>Web API is now active!</strong> This plugin provides RESTful API endpoints for accessing nopCommerce data.
</div>

<h3>Available API Endpoints</h3>

<h4>Authentication</h4>
<ul>
    <li><code>POST /api/auth/login</code> - Authenticate and get JWT token</li>
</ul>

<h4>Products</h4>
<ul>
    <li><code>GET /api/products</code> - Get all products (paginated)</li>
    <li><code>GET /api/products/{id}</code> - Get product by ID</li>
</ul>

<h4>Customers</h4>
<ul>
    <li><code>GET /api/customers</code> - Get all customers (paginated)</li>
    <li><code>GET /api/customers/{id}</code> - Get customer by ID</li>
</ul>

<h4>Orders</h4>
<ul>
    <li><code>GET /api/orders</code> - Get all orders (paginated)</li>
    <li><code>GET /api/orders/{id}</code> - Get order by ID</li>
</ul>

<h3>Authentication</h3>
<p>All API endpoints (except authentication) require JWT token authentication. Include the token in the Authorization header:</p>
<pre><code>Authorization: Bearer {your-jwt-token}</code></pre>

<h3>Usage Example</h3>
<p>First, authenticate to get a token:</p>
<pre><code>POST /api/auth/login
Content-Type: application/json

{
  "username": "admin",
  "password": "yourpassword"
}</code></pre>

<p>Then use the token to access protected endpoints:</p>
<pre><code>GET /api/products
Authorization: Bearer eyJhbGciOiJIUzI1NiIsInR5cCI6IkpXVCJ9...</code></pre>

